@model IEnumerable<S3G1_PVFAPP.Models.DoesBusinessIn>

@{
    ViewBag.Title = "Index";
    ViewBag.Active = "DoesBusinessIn";
}

<div class="contactus">
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-8 offset-md-2">
                <div class="title">
                    <h2>@ViewBag.Active</h2>

                </div>
            </div>
        </div>
    </div>
</div>
<p>
    @Html.ActionLink("Create New", "Create", "DoesBusinessIns", null, new { @class = "add-bt" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CreatedDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Customer.CustomerName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Territory.TerritoryName)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CreatedDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Customer.CustomerName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Territory.TerritoryName)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id1 = item.Customer.CustomerName, id2 = item.Territory.TerritoryName }) |
                @Html.ActionLink("Details", "Details", new { id1 = item.Customer.CustomerName, id2 = item.Territory.TerritoryName }) |
                @Html.ActionLink("Delete", "Delete", new { id1 = item.Customer.CustomerName, id2 = item.Territory.TerritoryName })
            </td>
        </tr>
    }

</table>
