@model IEnumerable<S3G1_PVFAPP.Models.Territory>

@{
    ViewBag.Title = "Index";
    ViewBag.Active = "Territories";
}

<div class="contactus">
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-8 offset-md-2">
                <div class="title">
                    <h2>Territories</h2>

                </div>
            </div>
        </div>
    </div>
</div>
<p>
    @Html.ActionLink("Create New", "Create", "Skills", null, new { @class = "add-bt" }) &nbsp
    @Html.ActionLink("Sort", "Index", "Skills", new { sort = 1 }, new { @class = "add-bt" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TerritoryName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TerritoryName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TerritoryID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TerritoryID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TerritoryID })
        </td>
    </tr>
}

</table>
