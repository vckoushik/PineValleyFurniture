@model IEnumerable<S3G1_PVFAPP.Models.WorkCenter>

@{
    ViewBag.Title = "Index";
    ViewBag.Active = "WorkCenter";
}

<div class="contactus">
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-8 offset-md-2">
                <div class="title">
                    <h2>Work Centers</h2>

                </div>
            </div>
        </div>
    </div>
</div>
<p>
    @Html.ActionLink("Create New", "Create", "WorkCenters", null, new { @class = "add-bt" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.WorkCenterLocation)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.WorkCenterLocation)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.WorkCenterID.ToString() }) |
                @Html.ActionLink("Details", "Details", new { id = item.WorkCenterID }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.WorkCenterID })
            </td>
        </tr>
    }

</table>
